// ============ Constants ============
// These are the only remaining exports from the old Zustand store.
// All state is now owned by WASM; React reads it via WasmEngine getters.

export const NUM_CHANNELS = 8;
export const VISIBLE_ROWS = 8;
export const VISIBLE_COLS = 16;
