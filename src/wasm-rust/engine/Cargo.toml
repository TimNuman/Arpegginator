[package]
name = "arp3-engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
libm = "0.2"  # f32 math (sqrt, sin, cos, atan2) for no_std

[build-dependencies]
fontdue = "0.9"
